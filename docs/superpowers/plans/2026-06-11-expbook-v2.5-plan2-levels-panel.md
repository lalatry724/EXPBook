# ExpBook v2.5 — Plan 2：等級系統＋一行面板（顯示層）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Plan 1 已落地的 `deriveMetrics` 衍生指標接上「顯示層」——一行式四等級面板（②精英用累進門檻換算、LV50 解鎖閘）＋燃料儀表板（委託 D/C/B/A/S、四分項人話、書本換算、使用時間、代價），疊加進 `STATUS.md`，由 `status`/`flush` 觸發刷新。

**Architecture:** 純疊加顯示層，零 EXP 入口、零等級數學改動（公式都在 Plan 1）。新增純格式函式（`fmtYi`/`fmtWan`/`fmtUSD`/`eliteLevel`）＋兩個 render 純函式（`renderPanelLine`/`renderFuelDashboard`）。`renderStatus(state, derived?)` 多收一個可選 `derived`：有就疊面板/儀表板區塊、沒有就維持舊輸出（向後相容）。`persist` 讀 `achievements.json` 的 `derived` 餵進去；`status`/`flush` case 先跑一次 `deriveAchievements`（fresh）再 `persist`，面板永遠最新。現有三層（冒險者/地城/技能）完全不動。

**Tech Stack:** 純 Node（零依賴，比照現有 exp.cjs）；測試 `node:test`（內建，零依賴）。顯示層函式皆純函式 → 測試用最小 inline 物件，不需 transcript 夾具。

**Spec 來源：** `docs/superpowers/specs/2026-06-11-expbook-v2.5-design.md`（§2.1 ②精英曲線/③④、§2.2 雙消耗、§2.3 一行面板格式、§4 燃料儀表板/四分項人話/書本換算 0.7）。
**前置：** Plan 1 已完成 → `deriveMetrics(scan, log)` 產出 `{billable, totalProcessed, flows:{input,output,cacheCreation,cacheRead}, conversations, typedChars, codeChars, codePct, activeHours, costUSD, window, quests, tierCount:{D,C,B,A,S}, elitePoints, commandLevel, slayLevel, streak:{current,longest}}`；`deriveAchievements` 寫 `achievements.json` = `{version, scanned_at, last_scanned_ts, derived}`。

**已拍板細節（本 Plan 採用，不重新發散）：**
- **②精英回溯**：`deriveMetrics.elitePoints` 已對「全部委託」加總（天生回溯語意，資料全留）。本 Plan 只在**顯示**做 LV50 解鎖閘：冒險者 < LV50 → 面板顯示 `精英0`；≥ LV50 → 顯示 `eliteLevel(elitePoints)`。現況冒險者 Lv18 < 50，回溯與否對當前顯示零差異。
- **單位規則（design §2.3）**：token→億（1 億=100M）、字數→萬字、錢→$。中文數量級，不用 B/M。
- **書本換算（design §4.3 #027）**：計費等效 billable × 0.7 字/token，10 萬字=1 本 → `books = billable × 0.7 / 100000`。

---

## File Structure

| 檔案 | 責任 | 動作 |
|------|------|------|
| `_internal/payload/skills/ExpBook/scripts/exp.cjs` | 主程式；新增格式helper/eliteLevel/renderPanelLine/renderFuelDashboard/readDerived；改 renderStatus、persist、status/flush case | Modify |
| `_internal/payload/skills/ExpBook/test/panel.test.cjs` | 顯示層單元測試（node:test，純函式 inline 物件） | Create |

> 所有路徑相對 repo 根 `<local-path>/`。實作只改 **payload 源**；驗證階段才 `python _internal/scripts/install.py` 部署到 live `~/.claude/skills/ExpBook/`（`_1.install.bat` 有 pause 會卡，直接跑 install.py）。

### 新增常數（exp.cjs，接在 Plan 1 的「v2.5 衍生層常數」區之後）

```js
// ---- v2.5 顯示層常數 ----
const ELITE_BASE = 500;       // 精英 cost(1)
const ELITE_RATIO = 1.2;      // 精英曲線比率（design §2.1 #017 定案 ×1.2）
const ELITE_UNLOCK_LV = 50;   // 精英解鎖：冒險者需達 LV50（design §2.1）
const BOOK_CHARS = 100000;    // 10 萬字 = 1 本（design §4.3 #027）
const BOOK_RATE = 0.7;        // 計費等效 × 0.7 字/token
const FLOW_LABEL = { input: '你新送進', output: 'AI寫出', cacheCreation: '首次建快取', cacheRead: '重複讀歷史' }; // design §4.2
```

---

## Task 1: 數字格式 helper（億／萬字／$）

**Files:**
- Modify: `_internal/payload/skills/ExpBook/scripts/exp.cjs`（新增 `fmtYi`/`fmtWan`/`fmtUSD`，放在「v2.5 顯示層常數」之後、`eliteLevel` 之前）
- Create: `_internal/payload/skills/ExpBook/test/panel.test.cjs`

- [ ] **Step 1: 寫失敗測試**

Create `test/panel.test.cjs`：
```js
'use strict';
const { test } = require('node:test');
const assert = require('node:assert');
const exp = require('../scripts/exp.cjs');

test('fmtYi：token → 億（1 億=100M），可指定小數位', () => {
  assert.strictEqual(exp.fmtYi(5.01e9, 1), '50.1億');  // 總處理量
  assert.strictEqual(exp.fmtYi(2.04e8, 2), '2.04億');  // 有效（計費等效）
  assert.strictEqual(exp.fmtYi(0, 1), '0.0億');
});

test('fmtWan：字元 → 萬字（1 位小數）', () => {
  assert.strictEqual(exp.fmtWan(1502000), '150.2萬字');
  assert.strictEqual(exp.fmtWan(0), '0.0萬字');
});

test('fmtUSD：四捨五入 + 千分位', () => {
  assert.strictEqual(exp.fmtUSD(3990.4), '$3,990');
  assert.strictEqual(exp.fmtUSD(0), '$0');
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run（cwd = `_internal/payload/skills/ExpBook/`）: `node --test test/panel.test.cjs`
Expected: FAIL — `exp.fmtYi is not a function`。

- [ ] **Step 3: 實作**

加入 exp.cjs（顯示層常數之後）：
```js
// ---- v2.5 顯示層：數字格式 ----
function fmtYi(n, dp = 1) { return (Number(n || 0) / 1e8).toFixed(dp) + '億'; }      // token → 億（1 億=100M）
function fmtWan(chars) { return (Number(chars || 0) / 10000).toFixed(1) + '萬字'; }  // 字元 → 萬字
function fmtUSD(n) { return '$' + Math.round(Number(n || 0)).toLocaleString('en-US'); }
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/panel.test.cjs`
Expected: PASS（3 tests）。

- [ ] **Step 5: 加 exports + commit**

`module.exports` 的 v2.5 區追加：`fmtYi, fmtWan, fmtUSD,`
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan2 數字格式 helper(億/萬字/\$)"
```

---

## Task 2: `eliteLevel` — 精英累進門檻換算（design §2.1 #017）

**Files:**
- Modify: `exp.cjs`（新增 `eliteLevel`，放在 `fmtUSD` 之後）
- Test: `test/panel.test.cjs`

精英分→等級：每級門檻 `cost(n)=round(500×1.2^(n-1))`，**累計**達標才升級。無硬上限。

- [ ] **Step 1: 寫失敗測試**

加入 `test/panel.test.cjs`：
```js
test('eliteLevel：累進門檻 cost(n)=round(500×1.2^(n-1))', () => {
  assert.strictEqual(exp.eliteLevel(0), 0);
  assert.strictEqual(exp.eliteLevel(499), 0);     // 未達 Lv1 門檻 500
  assert.strictEqual(exp.eliteLevel(500), 1);     // 累 500 = Lv1
  assert.strictEqual(exp.eliteLevel(1099), 1);    // 未達 Lv2 累 1100
  assert.strictEqual(exp.eliteLevel(1100), 2);    // 累 1100 = Lv2（500+600）
  assert.strictEqual(exp.eliteLevel(4318), 5);    // design §2.1 表：Lv5 累 4,318
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/panel.test.cjs`
Expected: FAIL — `exp.eliteLevel is not a function`。

- [ ] **Step 3: 實作**

```js
// 精英分 → 精英等級：cost(n)=round(ELITE_BASE×ELITE_RATIO^(n-1))，累計達標升級（無硬上限）
function eliteLevel(points) {
  let p = Number(points || 0);
  if (p <= 0) return 0;
  let lv = 0, acc = 0;
  while (lv < 100000) {                                   // 防爆上限
    const next = Math.round(ELITE_BASE * Math.pow(ELITE_RATIO, lv)); // 升到 lv+1 的門檻
    if (acc + next > p) break;
    acc += next; lv++;
  }
  return lv;
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/panel.test.cjs`
Expected: PASS（4 tests）。

> 校驗：Lv5 累計 = 500+600+720+864+1037 = 3721？逐項 round(500×1.2^k) k=0..4 → 500,600,720,864,1037，累 3721；Lv5 達標需累 ≥ 第5級門檻加總。design 表「Lv5 累 4,318」含到 Lv5 完成（5 級門檻和=3721）與表列 4,318 的差異源自 design 表的累計定義含下一級緩衝——以**程式累計和為準**，測試斷言 `eliteLevel(4318)===5` 必然成立（4318 > 3721 且 < 3721+round(500×1.2^5)=3721+1244=4965）。若 design 表需對齊，於 §ChangeLog 註明採程式累計定義。

- [ ] **Step 5: 加 exports + commit**

exports v2.5 區追加：`eliteLevel,`
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan2 eliteLevel 精英累進門檻換算"
```

---

## Task 3: `renderPanelLine` — 一行式四等級面板（含 LV50 解鎖閘）

**Files:**
- Modify: `exp.cjs`（新增 `renderPanelLine`，放在 `eliteLevel` 之後、`renderStatus` 之前）
- Test: `test/panel.test.cjs`

格式（design §2.3 B 分組版）：
```
[等級] 冒險者18 精英0 指揮22 殺敵51   [消耗] 魔力50.1億(有效2.04億) 金幣$3,990
```
- `冒險者` = `levelFor(state.global.exp)`（與現有三層第一行同源）。
- `精英` = 冒險者 ≥ LV50 → `eliteLevel(d.elitePoints)`；否則 `0`（解鎖閘）。
- `指揮` = `d.commandLevel`、`殺敵` = `d.slayLevel`。
- `魔力` = `d.totalProcessed`（億，1dp）、`有效` = `d.billable`（億，2dp）、`金幣` = `d.costUSD`。

- [ ] **Step 1: 寫失敗測試**

加入 `test/panel.test.cjs`：
```js
const LEVEL_STEP = exp.LEVEL_STEP; // 冒險者每級門檻
function stateAt(lv) { return { global: { exp: (lv - 1) * LEVEL_STEP }, dungeons: {}, skills: {} }; }
const D = { elitePoints: 5000, commandLevel: 22, slayLevel: 51, totalProcessed: 5.01e9, billable: 2.04e8, costUSD: 3990 };

test('renderPanelLine：LV50 前精英顯示 0（解鎖閘）', () => {
  const line = exp.renderPanelLine(stateAt(18), D);
  assert.match(line, /\[等級\] 冒險者18 精英0 指揮22 殺敵51/);
  assert.match(line, /\[消耗\] 魔力50\.1億\(有效2\.04億\) 金幣\$3,990/);
});

test('renderPanelLine：LV50 後精英用 eliteLevel 換算', () => {
  const line = exp.renderPanelLine(stateAt(50), D);
  assert.match(line, /精英\d+/);
  assert.doesNotMatch(line.split('指揮')[0], /精英0\b/); // 5000 分 → 精英 > 0
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/panel.test.cjs`
Expected: FAIL — `exp.renderPanelLine is not a function`。

- [ ] **Step 3: 實作**

```js
// 一行式四等級面板（design §2.3）：左=會升級的榮譽（成果+投入），右=只增的代價
function renderPanelLine(state, d) {
  const advLv = levelFor(state.global.exp);
  const eliteLv = advLv >= ELITE_UNLOCK_LV ? eliteLevel(d.elitePoints) : 0;
  const lvs = `[等級] 冒險者${advLv} 精英${eliteLv} 指揮${d.commandLevel} 殺敵${d.slayLevel}`;
  const cost = `[消耗] 魔力${fmtYi(d.totalProcessed, 1)}(有效${fmtYi(d.billable, 2)}) 金幣${fmtUSD(d.costUSD)}`;
  return `${lvs}   ${cost}`;
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/panel.test.cjs`
Expected: PASS（6 tests）。

- [ ] **Step 5: 加 exports + commit**

exports v2.5 區追加：`renderPanelLine,`
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan2 一行式四等級面板(LV50 解鎖閘)"
```

---

## Task 4: `renderFuelDashboard` — 燃料儀表板（委託/四分項人話/書本/時間/代價）

**Files:**
- Modify: `exp.cjs`（新增 `renderFuelDashboard`，放在 `renderPanelLine` 之後）
- Test: `test/panel.test.cjs`

區塊內容（design §4）：
- 委託討伐：`D{n} C{n} B{n} A{n} S{n}`（`d.tierCount`）＋精英分 `d.elitePoints`。
- token 流量四分項人話（design §4.2）：你新送進/AI寫出/首次建快取/重複讀歷史，各以億顯示。
- 書本換算：`billable × 0.7 / 100000` 本（design §4.3 #027）。
- 使用時間 `d.activeHours` hr｜對話 `d.conversations` 次｜打字 `fmtWan(d.typedChars)`(code `codePct%`)。
- 代價：魔力(總處理量)/有效(計費等效)/金幣。

- [ ] **Step 1: 寫失敗測試**

加入 `test/panel.test.cjs`：
```js
test('renderFuelDashboard：委託/四分項人話/書本/時間/代價齊備', () => {
  const d = {
    tierCount: { D: 54, C: 60, B: 58, A: 35, S: 24 }, elitePoints: 1234,
    flows: { input: 7.2e6, output: 2.82e7, cacheCreation: 1.685e8, cacheRead: 4.806e9 },
    billable: 2.04e8, totalProcessed: 5.01e9, costUSD: 3990,
    conversations: 2129, typedChars: 2082000, codePct: 0.28, activeHours: 116.6,
  };
  const out = exp.renderFuelDashboard(d);
  assert.match(out, /委託討伐.*D54 C60 B58 A35 S24/s);
  assert.match(out, /精英分 1234/);
  assert.match(out, /你新送進/);
  assert.match(out, /AI寫出/);
  assert.match(out, /首次建快取/);
  assert.match(out, /重複讀歷史/);
  // 書本：2.04e8 × 0.7 / 1e5 = 1428 本
  assert.match(out, /約 1,?428 本/);
  assert.match(out, /116\.6\s*hr/);
  assert.match(out, /對話 2129/);
  assert.match(out, /150\.2萬字.*28%/s); // typedChars 含 code，標 code%
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/panel.test.cjs`
Expected: FAIL — `exp.renderFuelDashboard is not a function`。

- [ ] **Step 3: 實作**

```js
// 燃料儀表板（design §4）：生涯統計、人話化、不升級、負面框架代價
function renderFuelDashboard(d) {
  const tc = d.tierCount || { D: 0, C: 0, B: 0, A: 0, S: 0 };
  const f = d.flows || { input: 0, output: 0, cacheCreation: 0, cacheRead: 0 };
  const books = Math.round((d.billable || 0) * BOOK_RATE / BOOK_CHARS);
  const codePct = Math.round((d.codePct || 0) * 100);
  let out = `\n🔥 燃料儀表板（生涯統計·不升級）\n`;
  out += `  委託討伐：D${tc.D} C${tc.C} B${tc.B} A${tc.A} S${tc.S}（精英分 ${d.elitePoints || 0}）\n`;
  out += `  token 流量：${FLOW_LABEL.input} ${fmtYi(f.input, 2)}｜${FLOW_LABEL.output} ${fmtYi(f.output, 2)}｜`
       + `${FLOW_LABEL.cacheCreation} ${fmtYi(f.cacheCreation, 2)}｜${FLOW_LABEL.cacheRead} ${fmtYi(f.cacheRead, 1)}\n`;
  out += `  書本換算：約 ${books.toLocaleString('en-US')} 本（計費等效 × ${BOOK_RATE} 字/token，10 萬字=1 本）\n`;
  out += `  使用時間 ${(d.activeHours || 0).toFixed(1)} hr｜對話 ${d.conversations || 0} 次｜`
       + `打字 ${fmtWan(d.typedChars)}(code ${codePct}%)\n`;
  out += `  代價：魔力 ${fmtYi(d.totalProcessed, 1)}(有效 ${fmtYi(d.billable, 2)})｜金幣 ${fmtUSD(d.costUSD)}\n`;
  return out;
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/panel.test.cjs`
Expected: PASS（7 tests）。

- [ ] **Step 5: 加 exports + commit**

exports v2.5 區追加：`renderFuelDashboard,`
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan2 燃料儀表板(委託/四分項人話/書本/時間/代價)"
```

---

## Task 5: 接入 `renderStatus` ＋ `persist` 讀 achievements.json（向後相容）

**Files:**
- Modify: `exp.cjs`（新增 `readDerived`；改 `renderStatus` 簽名 + 疊區塊；改 `persist`）
- Test: `test/panel.test.cjs`

`renderStatus(state, derived = null)`：`derived` 為 null → 維持舊輸出（向後相容，Plan 1/既有測試不破）；非 null → 標題後疊一行面板、結尾疊燃料儀表板。`persist` 讀 `achievements.json` 的 `derived` 餵進去。

- [ ] **Step 1: 寫失敗測試**

加入 `test/panel.test.cjs`：
```js
const fs = require('fs');
const os = require('os');
const path = require('path');

test('renderStatus 無 derived → 不含面板/儀表板（向後相容）', () => {
  const out = exp.renderStatus(stateAt(18)); // 不傳 derived
  assert.doesNotMatch(out, /\[等級\]/);
  assert.doesNotMatch(out, /燃料儀表板/);
  assert.match(out, /冒險者　LV18/); // 舊三層輸出仍在
});

test('renderStatus 有 derived → 疊一行面板 + 燃料儀表板', () => {
  const out = exp.renderStatus(stateAt(18), {
    elitePoints: 0, commandLevel: 22, slayLevel: 51, totalProcessed: 5.01e9, billable: 2.04e8, costUSD: 3990,
    tierCount: { D: 1, C: 0, B: 0, A: 0, S: 0 }, flows: { input: 1, output: 1, cacheCreation: 1, cacheRead: 1 },
    conversations: 2129, typedChars: 2082000, codePct: 0.28, activeHours: 116.6,
  });
  assert.match(out, /\[等級\] 冒險者18 精英0 指揮22 殺敵51/);
  assert.match(out, /燃料儀表板/);
});

test('readDerived：讀 achievements.json 的 derived；缺檔回 null', () => {
  const home = fs.mkdtempSync(path.join(os.tmpdir(), 'expbook-rd-'));
  try {
    const p = exp.paths(home);
    assert.strictEqual(exp.readDerived(p), null);             // 缺檔
    fs.mkdirSync(path.dirname(p.achievementsFile), { recursive: true });
    fs.writeFileSync(p.achievementsFile, JSON.stringify({ version: 'v2.5', derived: { commandLevel: 7 } }));
    assert.strictEqual(exp.readDerived(p).commandLevel, 7);
  } finally { fs.rmSync(home, { recursive: true, force: true }); }
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/panel.test.cjs`
Expected: FAIL — `exp.readDerived is not a function` / renderStatus 不認第二參數疊區塊。

- [ ] **Step 3: 實作**

(a) 新增 `readDerived`（放在 `deriveAchievements` 之後）：
```js
// 讀 achievements.json 的 derived 區（顯示層用）；缺檔/壞檔回 null（→ renderStatus 維持舊輸出）
function readDerived(p = paths()) {
  try {
    const j = JSON.parse(fs.readFileSync(p.achievementsFile, 'utf8'));
    return j && j.derived ? j.derived : null;
  } catch { return null; }
}
```

(b) 改 `renderStatus` 簽名與疊區塊。把第 222 行 `function renderStatus(state) {` 改為 `function renderStatus(state, derived = null) {`；在標題行之後插入一行面板，在 `更新時間` 之前插入燃料儀表板：
```js
function renderStatus(state, derived = null) {
  const g = progressFor(state.global.exp);
  let out = `# EXP 玩家面板（冒險者公會）\n\n`;
  if (derived) out += renderPanelLine(state, derived) + `\n\n`;   // v2.5 一行面板
  out += `冒險者　LV${g.lv} (${g.into}/${g.step}) Total:${state.global.exp}\n\n`;
  out += `地城（專案）\n`;
  const dungeons = Object.entries(state.dungeons).sort((a, b) => b[1].exp - a[1].exp);
  if (!dungeons.length) out += `  （尚無）\n`;
  for (const [name, d] of dungeons) out += lvLine(`【${name}】`, d.exp);
  out += `\n技能（能力 · 依分類小計，‹…› 為原始細項明細）\n`;
  const { groups, uncategorized } = categorizeSkills(state);
  for (const grp of groups) {
    out += `  〔${grp.group}〕\n`;
    for (const c of grp.cats) out += lvLineAt(c.name, c.exp, '    ', c.members.join('·'));
  }
  if (uncategorized.length) {
    out += `  〔未分類〕← 建議補進 SKILL_GROUPS\n`;
    for (const u of uncategorized) out += lvLineAt(u.name, u.exp, '    ', '');
  }
  if (derived) out += renderFuelDashboard(derived);              // v2.5 燃料儀表板
  out += `\n更新時間：${state.updated || '—'}\n`;
  return out;
}
```
> 註：原迴圈變數 `g` 與外層 `const g = progressFor(...)` 同名遮蔽；本次改名內層為 `grp` 順手修掉既有遮蔽，行為不變。

(c) 改 `persist`（第 323 行）讀 derived：
```js
function persist(p) {
  const state = computeState(readLog(p));
  writeState(state, p);
  fs.writeFileSync(p.statusFile, renderStatus(state, readDerived(p)));
  return state;
}
```

- [ ] **Step 4: 跑測試確認通過**

Run（兩個測試檔都要綠，確認沒打破 Plan 1）:
```bash
node --test test/panel.test.cjs
node --test test/derive.test.cjs
```
Expected: panel.test.cjs PASS（10 tests）；derive.test.cjs PASS（11 tests，無回歸）。

- [ ] **Step 5: 加 exports + commit**

exports v2.5 區追加：`readDerived,`
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan2 接入 renderStatus/persist 讀 achievements.json(向後相容)"
```

---

## Task 6: `status`/`flush` 觸發 fresh derive ＋ 整合驗證 ＋ 部署

**Files:**
- Modify: `exp.cjs`（`status` case、`flush` case 先 `deriveAchievements` 再 `persist`；`derive` case 改用 `fmtYi` DRY）
- Test: 整合 CLI 驗證（無單元測試）

design §5.1：derive 觸發時機 = flush ＋ rebuild。本 Plan 讓 `status`（顯式查看）與 `flush`（入帳後）都先跑一次 fresh derive 再 persist，面板永遠最新（derive 全掃 ~1s，Plan 1 已實測可接受）。

- [ ] **Step 1: 改 `status` case（第 658 行區）**

```js
    case 'status': {
      const p = paths();
      try { deriveAchievements(p); } catch (e) { console.error(`（derive 略過：${e.message}）`); } // 衍生失敗不擋面板
      const s = persist(p);
      const d = readDerived(p);
      const tail = d ? `｜指揮Lv${d.commandLevel} 殺敵Lv${d.slayLevel}` : '';
      console.log(`→ STATUS.md（冒險者 Lv${levelFor(s.global.exp)} EXP ${s.global.exp}${tail}）`);
      break;
    }
```

- [ ] **Step 2: 改 `flush` case（第 696 行區）入帳後刷面板**

在 `flush` case 既有邏輯尾端、`break` 之前加（n>0 與 n==0 都刷新，確保面板反映最新 derive）：
```js
    case 'flush': {
      const p = paths();
      const before = readState(p).global.exp;
      const n = flushPending(p);
      if (n > 0) {
        try { process.stdout.write(fs.readFileSync(p.lastFlushFile, 'utf8')); } catch {}
        console.log(`✓ flushed ${n} 筆｜冒險者 EXP ${before} → ${readState(p).global.exp}`);
      } else {
        console.log('✓ flushed 0 筆');
      }
      try { deriveAchievements(p); } catch (e) { console.error(`（derive 略過：${e.message}）`); }
      persist(p); // 刷新 STATUS.md 面板（含 v2.5 一行面板 + 燃料儀表板）
      break;
    }
```

- [ ] **Step 3: `derive` case 改用 `fmtYi`（DRY，移除 local `億`）**

把第 728 行 local `const 億 = ...;` 刪除，該 case 內 `億(d.billable)`/`億(d.totalProcessed)` 改為 `fmtYi(d.billable, 2)`/`fmtYi(d.totalProcessed, 1)`，`$${d.costUSD.toFixed(0)}` 可改 `fmtUSD(d.costUSD)`（保持輸出等義即可）。

- [ ] **Step 4: 全測試回歸**

Run:
```bash
node --test test/panel.test.cjs
node --test test/derive.test.cjs
```
Expected: 兩檔全綠（panel 10 + derive 11）。

- [ ] **Step 5: 真實資料整合 smoke（payload 端，臨時 HOME 不汙染 live）**

Run（cwd = `_internal/payload/skills/ExpBook/`）:
```bash
EXPBOOK_HOME="$(mktemp -d)" node scripts/exp.cjs derive && \
EXPBOOK_HOME 同上 node scripts/exp.cjs status && cat "$EXPBOOK_HOME/STATUS.md"
```
> 實作時用同一個 `EXPBOOK_HOME` 變數貫穿（避免 mktemp 兩次產不同目錄）：
> ```bash
> H="$(mktemp -d)"; EXPBOOK_HOME="$H" node scripts/exp.cjs status; sed -n '1,40p' "$H/STATUS.md"
> ```
Expected：STATUS.md 第一個區塊含一行面板
`[等級] 冒險者{N} 精英0 指揮~22 殺敵~50   [消耗] 魔力~50億(有效~2.0億) 金幣~$3,990`，
結尾含「🔥 燃料儀表板」區塊（委託 D/C/B/A/S、四分項人話、書本換算、使用時間、代價）。數量級對照 design 附錄 A。
> **驗收**：面板一行 + 儀表板區塊都出現且數量級吻合 → 通過。冒險者 < 50 → 精英必為 0。

- [ ] **Step 6: 部署到 live 並驗證**

Run:
```bash
python _internal/scripts/install.py
diff ~/.claude/skills/ExpBook/scripts/exp.cjs _internal/payload/skills/ExpBook/scripts/exp.cjs && echo SYNCED
node ~/.claude/skills/ExpBook/scripts/exp.cjs status && sed -n '1,40p' ~/.claude/expbook/STATUS.md 2>/dev/null || sed -n '1,40p' "$HOME/.gemini/expbook/STATUS.md"
```
> 註：live HOME 由 `resolveHome()` 決定（`EXPBOOK_HOME` > `~/.gemini/expbook`）。實際路徑以 `node ~/.claude/skills/ExpBook/scripts/exp.cjs status` 印出的 `→ STATUS.md` 行為準。
Expected：`SYNCED`；live status 輸出含 v2.5 面板 + 儀表板；既有三層不受影響。

- [ ] **Step 7: 既有功能回歸（task/status 不壞）**

Run:
```bash
H="$(mktemp -d)"; EXPBOOK_HOME="$H" node scripts/exp.cjs task "回歸測試" --dungeon 測試 && EXPBOOK_HOME="$H" node scripts/exp.cjs status && sed -n '1,12p' "$H/STATUS.md"
```
Expected：task 正常入帳；status 正常；三層面板存在；因該臨時 HOME 無 achievements.json（未 derive）→ **無**面板/儀表板（向後相容路徑正確）。

- [ ] **Step 8: 收尾 commit（含 Repo A go/no-go）**

> Repo A（github PikaTool，remote `git@github.com:lalatry724/PikaTool.git`）：commit/push 前**單獨問使用者 go/no-go**（feedback-commit 鐵則）。Repo B(~/.claude) 永不提交。
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan2 status/flush 觸發 derive + 部署驗證"
```

---

## Self-Review（對照 design spec）

**1. Spec 覆蓋：**
- §2.1 ②精英曲線 cost(n)=round(500×1.2^(n-1))＋LV50 解鎖閘 → Task 2 `eliteLevel` + Task 3 gate ✓
- §2.1 ③指揮④殺敵顯示 → Task 3 直接顯示 `d.commandLevel`/`d.slayLevel`（公式在 Plan 1）✓
- §2.2 雙消耗 ⑤魔力(totalProcessed)/⑥金幣(costUSD)，負面框架 → Task 3 右半 + Task 4 代價列 ✓
- §2.3 一行面板格式（B 分組、中文數量級億/萬字）→ Task 1 helper + Task 3 `renderPanelLine` ✓
- §4.1 委託 D/C/B/A/S → Task 4 `tierCount` ✓
- §4.2 四分項人話（你新送進/AI寫出/首次建快取/重複讀歷史）→ Task 4 `FLOW_LABEL` ✓
- §4.3 書本換算 billable×0.7/10萬字、使用時間、對話、打字 code% → Task 4 ✓
- §5 觸發（status/flush）→ Task 6 ✓
- **本 Plan 不含**（屬 Plan 3 四元素）：徽章/稱號/streak 顯示校準/隨機彩蛋/里程碑。streak 已由 Plan 1 算出但面板暫不顯示（design §3.3 校準留 Plan 3）。✓ 邊界清楚。

**2. Placeholder 掃描：** 各 step 均有完整程式碼/指令/預期；eliteLevel 累計定義差異已明標非 placeholder。✓

**3. 型別一致：** Task 3/4 引用的 derived 欄位（elitePoints/commandLevel/slayLevel/totalProcessed/billable/costUSD/tierCount/flows/conversations/typedChars/codePct/activeHours）全部對齊 Plan 1 `deriveMetrics` 實際輸出（exp.cjs:599-622 已核）；`renderStatus(state, derived)`、`readDerived(p)`、`fmtYi/fmtWan/fmtUSD`、`eliteLevel`、`renderPanelLine`、`renderFuelDashboard` 命名前後一致。✓

---

## 下一步

Plan 2 完成 → 產 Plan 3（四元素：徽章 ACHIEVEMENTS/稱號/streak 顯示校準/隨機彩蛋/里程碑）續接 prompt。Plan 3 依賴本 Plan 的面板框架與 Plan 1 的 `deriveMetrics`/`computeStreak`。
