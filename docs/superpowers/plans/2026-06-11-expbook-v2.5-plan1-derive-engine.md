# ExpBook v2.5 — Plan 1：衍生引擎（地基）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 為 ExpBook 新增「transcript 衍生層」——掃 `~/.claude/projects/*/*.jsonl` 的 token usage ＋ 自身 `log.jsonl`，算出 v2.5 全部等級/儀表板所需的衍生指標，落地成可重算的 `achievements.json`，並為每筆 event 補 `cwd` 欄。

**Architecture:** 純疊加，不動現有 log→computeState→三層邏輯。新增三層純函式：`scanTranscripts(root)` 掃原始 usage → `deriveMetrics(scan, log)` 算衍生指標 → `deriveAchievements(p)` 寫快取。新增 CLI `derive` 指令獨立可跑可驗。本 Plan **只產數據層**，面板顯示與徽章邏輯留 Plan 2/3。

**Tech Stack:** 純 Node（零依賴，比照現有 exp.cjs）；測試用 `node:test`（內建，零依賴）。

**Spec 來源：** `docs/superpowers/specs/2026-06-11-expbook-v2.5-design.md`（§1 度量基準、§2.1 ③④公式/②精英分、§4 委託區間歸屬/界線/四分項、§5 衍生引擎、§6 cwd）。

**度量鐵則（design §1）：** 計費等效 token = input + output + cache_creation（**排除 cache_read**）。

---

## File Structure

| 檔案 | 責任 | 動作 |
|------|------|------|
| `_internal/payload/skills/ExpBook/scripts/exp.cjs` | 主程式；新增掃描/衍生/derive 指令/常數 | Modify |
| `_internal/payload/skills/ExpBook/test/derive.test.cjs` | 衍生引擎單元測試（node:test） | Create |
| `_internal/payload/skills/ExpBook/test/fixtures.cjs` | 測試夾具：寫假 transcript/log 到 temp dir | Create |

> 所有路徑相對 repo 根 `<local-path>/`。實作只改 **payload 源**；驗證階段才 `_1.install.bat` 部署到 live `~/.claude/skills/ExpBook/`。

### 新增常數（exp.cjs 頂部，接在現有常數後）

```js
// ---- v2.5 衍生層常數 ----
const BILLABLE = (u) => (u.input_tokens||0) + (u.output_tokens||0) + (u.cache_creation_input_tokens||0); // 排除 cache_read
// 定價（每百萬 token，2026-06；查 claude-api skill 為準，變動只重算展示欄、不影響等級）
const PRICING = {
  opus:   { in: 5,  out: 25, cc: 6.25, cr: 0.5 },
  sonnet: { in: 3,  out: 15, cc: 3.75, cr: 0.3 },
  haiku:  { in: 1,  out: 5,  cc: 1.25, cr: 0.1 },
};
const ACTIVE_GAP_MS = 15 * 60000;           // 使用時間：相鄰訊息 gap<15 分才累加（design §4.3）
// 委託界線（計費等效 token；design §4.1，錨真實分位 p25/中位/p75/p90）
const QUEST_TIERS = [
  { tier: 'D', lo: 0,        hi: 170000 },
  { tier: 'C', lo: 170000,   hi: 330000 },
  { tier: 'B', lo: 330000,   hi: 760000 },
  { tier: 'A', lo: 760000,   hi: 1800000 },
  { tier: 'S', lo: 1800000,  hi: Infinity },
];
const ELITE_WEIGHT = { D: 1, C: 2, B: 3, A: 5, S: 8 }; // design §2.1 精英分權重
function projectsRoot() { return path.join(os.homedir(), '.claude', 'projects'); }
```

---

## Task 1: event 加 `cwd` 欄（#029）

**Files:**
- Modify: `_internal/payload/skills/ExpBook/scripts/exp.cjs`（`buildEvent`，約 L308-314）
- Create: `_internal/payload/skills/ExpBook/test/fixtures.cjs`
- Test: `_internal/payload/skills/ExpBook/test/derive.test.cjs`

- [ ] **Step 1: 建測試夾具**

Create `test/fixtures.cjs`：
```js
'use strict';
const fs = require('fs');
const os = require('os');
const path = require('path');

// 建一個臨時 EXPBOOK_HOME，回傳路徑；呼叫端負責清理
function tmpHome() {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'expbook-test-'));
  return dir;
}

// 建一個臨時 projects root，寫入指定的 transcript 檔
// sessions: [{ proj, file, lines: [obj,...] }]
function tmpProjects(sessions) {
  const root = fs.mkdtempSync(path.join(os.tmpdir(), 'expbook-proj-'));
  for (const s of sessions) {
    const dir = path.join(root, s.proj);
    fs.mkdirSync(dir, { recursive: true });
    const body = s.lines.map((o) => JSON.stringify(o)).join('\n') + '\n';
    fs.writeFileSync(path.join(dir, s.file), body);
  }
  return root;
}

// 組一筆 assistant transcript 訊息（帶 usage + model + timestamp）
function asstMsg(ts, model, { in: i = 0, out = 0, cc = 0, cr = 0 } = {}) {
  return { type: 'assistant', timestamp: ts, message: {
    role: 'assistant', model,
    usage: { input_tokens: i, output_tokens: out, cache_creation_input_tokens: cc, cache_read_input_tokens: cr },
  } };
}

// 組一筆 user transcript 訊息（純文字 or tool_result）
function userMsg(ts, text) {
  return { type: 'user', timestamp: ts, message: { role: 'user', content: text } };
}
function toolResultMsg(ts) {
  return { type: 'user', timestamp: ts, message: { role: 'user', content: [{ type: 'tool_result', content: 'x' }] } };
}

function rm(dir) { try { fs.rmSync(dir, { recursive: true, force: true }); } catch {} }

module.exports = { tmpHome, tmpProjects, asstMsg, userMsg, toolResultMsg, rm };
```

- [ ] **Step 2: 寫失敗測試**

Create `test/derive.test.cjs`：
```js
'use strict';
const { test } = require('node:test');
const assert = require('node:assert');
const path = require('path');
const fx = require('./fixtures.cjs');
const exp = require('../scripts/exp.cjs');

test('buildEvent 帶 cwd 欄＝process.cwd()', () => {
  const ev = exp.buildEvent('task', '測試', {});
  assert.strictEqual(ev.cwd, process.cwd());
});
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `node --test test/derive.test.cjs`（cwd = `_internal/payload/skills/ExpBook/`）
Expected: FAIL — `ev.cwd` 為 undefined。

- [ ] **Step 4: 實作**

在 `buildEvent`（exp.cjs）加 `cwd`：
```js
function buildEvent(kind, reason, { dungeon, skills } = {}) {
  const ev = { ts: now(), kind, reason };
  if (dungeon) ev.dungeon = dungeon;
  if (skills && skills.length) ev.skills = skills;
  ev.exp = EXP_OF[kind] ?? 0;
  ev.cwd = process.cwd(); // #029：記錄當下專案資料夾，與手動 dungeon tag 並存
  return ev;
}
```

- [ ] **Step 5: 跑測試確認通過**

Run: `node --test test/derive.test.cjs`
Expected: PASS（1 test）。

- [ ] **Step 6: Commit**

```bash
git add _internal/payload/skills/ExpBook/scripts/exp.cjs _internal/payload/skills/ExpBook/test/
git commit -m "feat(ExpBook): v2.5 Plan1 #029 event 加 cwd 欄 + 測試骨架"
```

---

## Task 2: `scanTranscripts(root)` — 掃 transcript 原始彙總

**Files:**
- Modify: `exp.cjs`（新增 `scanTranscripts`，放在「等級數學」區之後、CLI 之前）
- Test: `test/derive.test.cjs`

掃 root 下所有 `*.jsonl`，回傳原始彙總（token 分項 by-model、對話次數、打字字元、code 字元、所有訊息的 {ts,billable} 供區間 join、per-session/per-day 計費等效）。算法移植自已驗證的 `c:/tmp/{tokmetric,tokfuel,tokcount,tokscale}.cjs`。

- [ ] **Step 1: 寫失敗測試**

加入 `test/derive.test.cjs`：
```js
test('scanTranscripts 彙總 token/對話/打字（排除 tool_result 與 < 開頭）', () => {
  const root = fx.tmpProjects([{ proj: 'projA', file: 's1.jsonl', lines: [
    fx.asstMsg('2026-06-01T10:00:00.000Z', 'claude-opus-4-8', { in: 100, out: 200, cc: 300, cr: 9000 }),
    fx.userMsg('2026-06-01T10:01:00.000Z', '幫我修這個 bug'),                 // 6 字，計入
    fx.userMsg('2026-06-01T10:02:00.000Z', '```js\nconst x=1;\n```'),          // code fence
    fx.toolResultMsg('2026-06-01T10:03:00.000Z'),                              // 排除
    fx.userMsg('2026-06-01T10:04:00.000Z', '<system-reminder>x</system-reminder>'), // < 開頭排除
  ] }]);
  try {
    const s = exp.scanTranscripts(root);
    assert.strictEqual(s.tok.input, 100);
    assert.strictEqual(s.tok.output, 200);
    assert.strictEqual(s.tok.cacheCreation, 300);
    assert.strictEqual(s.tok.cacheRead, 9000);
    assert.strictEqual(s.billable, 600);          // 100+200+300，排除 cr
    assert.strictEqual(s.userTurns, 2);           // bug 那則 + code fence 那則
    assert.strictEqual(s.codeChars > 0, true);
    assert.strictEqual([...'幫我修這個 bug'].length <= s.userChars, true);
    assert.strictEqual(s.byModel['claude-opus-4-8'].input, 100);
    assert.strictEqual(s.messages.length >= 1, true); // 至少 assistant 那筆有 billable
  } finally { fx.rm(root); }
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/derive.test.cjs`
Expected: FAIL — `exp.scanTranscripts is not a function`。

- [ ] **Step 3: 實作 `scanTranscripts`**

加入 exp.cjs：
```js
// ---- v2.5 衍生層：掃 transcript 原始彙總 ----
function _txtOf(content) {
  if (typeof content === 'string') return content;
  if (Array.isArray(content)) {
    let s = '';
    for (const b of content) if (b && b.type === 'text' && typeof b.text === 'string') s += b.text;
    return s;
  }
  return '';
}
function _isToolResult(content) {
  return Array.isArray(content) && content.some((b) => b && b.type === 'tool_result');
}
function scanTranscripts(root = projectsRoot()) {
  const tok = { input: 0, output: 0, cacheCreation: 0, cacheRead: 0 };
  const byModel = {};
  const messages = [];                 // {ts:ms, billable} 供委託區間 join（僅含有 usage 的訊息）
  const tsList = [];                   // 所有訊息 timestamp（ms）供使用時間
  const perSession = [];               // 每檔計費等效
  const perDay = {};                   // 'YYYY-MM-DD' → 計費等效
  let userTurns = 0, userChars = 0, codeChars = 0, msgCount = 0, fileCount = 0;
  let minTs = null, maxTs = null;
  if (!fs.existsSync(root)) return _emptyScan();
  const walk = (dir) => {
    for (const e of fs.readdirSync(dir, { withFileTypes: true })) {
      const p = path.join(dir, e.name);
      if (e.isDirectory()) { walk(p); continue; }
      if (!e.name.endsWith('.jsonl')) continue;
      fileCount++;
      let sessBill = 0;
      for (const line of fs.readFileSync(p, 'utf8').split('\n')) {
        if (!line.trim()) continue;
        let o; try { o = JSON.parse(line); } catch { continue; }
        const msg = o.message || o;
        const role = msg.role || o.type;
        const u = msg.usage;
        const tsRaw = o.timestamp;
        const tsMs = tsRaw ? Date.parse(tsRaw) : null;
        if (tsMs != null && !Number.isNaN(tsMs)) {
          tsList.push(tsMs);
          if (minTs == null || tsMs < minTs) minTs = tsMs;
          if (maxTs == null || tsMs > maxTs) maxTs = tsMs;
        }
        if (u && role === 'assistant') {
          tok.input += u.input_tokens || 0;
          tok.output += u.output_tokens || 0;
          tok.cacheCreation += u.cache_creation_input_tokens || 0;
          tok.cacheRead += u.cache_read_input_tokens || 0;
          const m = msg.model || 'unknown';
          const bm = byModel[m] || (byModel[m] = { input: 0, output: 0, cacheCreation: 0, cacheRead: 0 });
          bm.input += u.input_tokens || 0; bm.output += u.output_tokens || 0;
          bm.cacheCreation += u.cache_creation_input_tokens || 0; bm.cacheRead += u.cache_read_input_tokens || 0;
          const b = BILLABLE(u);
          sessBill += b;
          if (tsMs != null) { messages.push({ ts: tsMs, billable: b }); perDay[tsRaw.slice(0, 10)] = (perDay[tsRaw.slice(0, 10)] || 0) + b; }
          msgCount++;
        }
        if (role === 'user' && msg.content != null && !_isToolResult(msg.content)) {
          const t = _txtOf(msg.content);
          if (t && !t.startsWith('<')) {            // 排除系統注入（design §4.4 startsWith('<') 粗濾）
            userTurns++; userChars += [...t].length;
            for (const f of (t.match(/```[\s\S]*?```/g) || [])) codeChars += [...f].length;
          }
        }
      }
      if (sessBill > 0) perSession.push(sessBill);
    }
  };
  walk(root);
  const billable = tok.input + tok.output + tok.cacheCreation;
  return { tok, byModel, billable, totalProcessed: billable + tok.cacheRead,
    messages, tsList, perSession, perDay, userTurns, userChars, codeChars,
    msgCount, fileCount, minTs, maxTs };
}
function _emptyScan() {
  return { tok: { input: 0, output: 0, cacheCreation: 0, cacheRead: 0 }, byModel: {}, billable: 0, totalProcessed: 0,
    messages: [], tsList: [], perSession: [], perDay: {}, userTurns: 0, userChars: 0, codeChars: 0,
    msgCount: 0, fileCount: 0, minTs: null, maxTs: null };
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/derive.test.cjs`
Expected: PASS（buildEvent + scanTranscripts，共 2 tests）。

- [ ] **Step 5: 加進 module.exports**

把 `scanTranscripts` 加入 exp.cjs 結尾 `module.exports`：
```js
  loadConfig, expDeltaOf,
  scanTranscripts, // v2.5 衍生層
```

- [ ] **Step 6: Commit**

```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan1 scanTranscripts 掃 transcript token/對話/打字彙總"
```

---

## Task 3: `deriveMetrics` 基礎指標（token 四分項/對話/打字/時間/成本）

**Files:**
- Modify: `exp.cjs`（新增 `activeHours`、`costOf`、`deriveMetrics`）
- Test: `test/derive.test.cjs`

- [ ] **Step 1: 寫失敗測試**

```js
test('activeHours 只累加 gap<15 分的間隔', () => {
  const tsList = [0, 5*60000, 10*60000, 30*60000].map(Number); // 0→5(計)、5→10(計)、10→30(20分,不計)
  assert.strictEqual(Math.round(exp.activeHours(tsList) * 60), 10); // 10 分鐘
});

test('costOf 依 model 分項定價', () => {
  const byModel = { 'claude-opus-4-8': { input: 1e6, output: 0, cacheCreation: 0, cacheRead: 0 } };
  assert.strictEqual(exp.costOf(byModel), 5); // opus input $5/M × 1M
});

test('deriveMetrics 算出四分項/對話/打字/時間/成本', () => {
  const root = fx.tmpProjects([{ proj: 'p', file: 's.jsonl', lines: [
    fx.asstMsg('2026-06-01T10:00:00.000Z', 'claude-opus-4-8', { in: 1e6, out: 0, cc: 0, cr: 0 }),
    fx.userMsg('2026-06-01T10:01:00.000Z', 'hello world'),
  ] }]);
  try {
    const m = exp.deriveMetrics(exp.scanTranscripts(root), []);
    assert.strictEqual(m.billable, 1e6);
    assert.strictEqual(m.flows.input, 1e6);
    assert.strictEqual(m.conversations, 1);
    assert.strictEqual(m.typedChars, [...'hello world'].length);
    assert.strictEqual(m.costUSD, 5);
  } finally { fx.rm(root); }
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/derive.test.cjs`
Expected: FAIL — `exp.activeHours is not a function`。

- [ ] **Step 3: 實作**

```js
// 使用時間：相鄰訊息 gap<15 分才累加，回傳小時
function activeHours(tsList) {
  const a = tsList.slice().sort((x, y) => x - y);
  let total = 0;
  for (let i = 1; i < a.length; i++) { const d = a[i] - a[i - 1]; if (d > 0 && d < ACTIVE_GAP_MS) total += d; }
  return total / 3600000;
}
// 等效成本 $：每 model 各欄 × 單價（model 名以 opus/sonnet/haiku 子字串匹配；未知→不計）
function costOf(byModel) {
  let usd = 0;
  for (const [model, v] of Object.entries(byModel)) {
    const key = /opus/i.test(model) ? 'opus' : /sonnet/i.test(model) ? 'sonnet' : /haiku/i.test(model) ? 'haiku' : null;
    if (!key) continue;
    const pr = PRICING[key];
    usd += (v.input * pr.in + v.output * pr.out + v.cacheCreation * pr.cc + v.cacheRead * pr.cr) / 1e6;
  }
  return usd;
}
// 衍生指標主函式（log = readLog() 結果；本 Task 只填基礎欄，委託/等級在 Task 4/5 補）
function deriveMetrics(scan, log) {
  return {
    billable: scan.billable,
    totalProcessed: scan.totalProcessed,
    flows: { input: scan.tok.input, output: scan.tok.output, cacheCreation: scan.tok.cacheCreation, cacheRead: scan.tok.cacheRead },
    conversations: scan.userTurns,
    typedChars: scan.userChars,
    codeChars: scan.codeChars,
    codePct: scan.userChars ? scan.codeChars / scan.userChars : 0,
    activeHours: activeHours(scan.tsList),
    costUSD: costOf(scan.byModel),
    window: { from: scan.minTs, to: scan.maxTs, files: scan.fileCount, messages: scan.msgCount },
  };
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/derive.test.cjs`
Expected: PASS（5 tests）。

- [ ] **Step 5: 加 exports + commit**

exports 追加 `activeHours, costOf, deriveMetrics`。
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan1 deriveMetrics 基礎指標(四分項/對話/打字/時間/成本)"
```

---

## Task 4: 委託區間歸屬 ＋ tier 分級 ＋ 精英分（design §4.1 / §2.1）

**Files:**
- Modify: `exp.cjs`（新增 `questsByTaskInterval`、`classifyTier`；擴充 `deriveMetrics`）
- Test: `test/derive.test.cjs`

委託 = task event 級 ＋ 區間 token 歸屬：把 task event 按 ts 排序，每筆 task 的委託 token = 「上一筆 task.ts（不含）到本筆 task.ts（含）」之間所有 transcript 訊息的計費等效 token 加總。跨 session 自動合併（純看時間區間）。中間無 task 的 token / 純探索→不形成委託（design §4.4 edge）。

> **注意：** event.ts 是本地時間字串（`YYYY-MM-DD HH:MM:SS`，見 `fmtTs`），transcript timestamp 是 UTC ISO。比較前都轉成 ms（`Date.parse`）。`fmtTs` 產的字串無時區，`Date.parse` 會當本地時間解析——與 transcript 的 UTC 經 `Date.parse` 後同為絕對 ms，可直接比大小。

- [ ] **Step 1: 寫失敗測試**

```js
test('classifyTier 依界線分級', () => {
  assert.strictEqual(exp.classifyTier(100000).tier, 'D');
  assert.strictEqual(exp.classifyTier(200000).tier, 'C');
  assert.strictEqual(exp.classifyTier(2000000).tier, 'S');
});

test('questsByTaskInterval 區間歸屬 + 跨 session 合併', () => {
  // 兩個 task event；transcript 訊息落在不同區間
  const t1 = exp.fmtTs(new Date(Date.UTC(2026,5,1,10,0,0)));
  const t2 = exp.fmtTs(new Date(Date.UTC(2026,5,1,12,0,0)));
  const log = [
    { ts: t1, kind: 'task', reason: '委託一' },
    { ts: t2, kind: 'task', reason: '委託二' },
  ];
  const scan = { messages: [
    { ts: Date.UTC(2026,5,1,9,30,0),  billable: 50000 },   // → 委託一（≤t1）
    { ts: Date.UTC(2026,5,1,11,0,0),  billable: 200000 },  // → 委託二（t1<..≤t2）
    { ts: Date.UTC(2026,5,1,13,0,0),  billable: 999 },     // → t2 之後，無 task，不形成委託
  ] };
  const quests = exp.questsByTaskInterval(scan, log);
  assert.strictEqual(quests.length, 2);
  assert.strictEqual(quests[0].billable, 50000);
  assert.strictEqual(quests[0].tier, 'D');
  assert.strictEqual(quests[1].billable, 200000);
  assert.strictEqual(quests[1].tier, 'C');
});
```

> 註：`fmtTs` 用本地時區格式化，再 `Date.parse` 解析回本地 ms。測試用 `Date.UTC` 建 transcript ms，兩者經 `Date.parse(fmtTs(...))` 會落在同一本地基準，區間比較成立（同機器同時區自洽）。

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/derive.test.cjs`
Expected: FAIL — `exp.classifyTier is not a function`。

- [ ] **Step 3: 實作**

```js
function classifyTier(billable) {
  for (const t of QUEST_TIERS) if (billable >= t.lo && billable < t.hi) return t;
  return QUEST_TIERS[QUEST_TIERS.length - 1];
}
// 把 transcript token 依 task event 時間區間歸成委託；回傳每筆 task 對應的委託
function questsByTaskInterval(scan, log) {
  const tasks = log.filter((e) => e.kind === 'task')
    .map((e) => ({ ev: e, ms: Date.parse(e.ts) }))
    .filter((x) => !Number.isNaN(x.ms))
    .sort((a, b) => a.ms - b.ms);
  if (!tasks.length) return [];
  const quests = tasks.map((t) => ({ ts: t.ev.ts, reason: t.ev.reason, dungeon: t.ev.dungeon || null, billable: 0 }));
  for (const m of scan.messages) {
    // 找第一筆 task.ms >= m.ts（含），歸給它；落在最後 task 之後者不歸（無 task → 不形成委託）
    let idx = -1;
    for (let i = 0; i < tasks.length; i++) { if (m.ts <= tasks[i].ms) { idx = i; break; } }
    if (idx >= 0) quests[idx].billable += m.billable;
  }
  for (const q of quests) q.tier = classifyTier(q.billable).tier;
  return quests;
}
```

擴充 `deriveMetrics` 結尾（在 `return` 物件內加）：
```js
  // ...既有欄位...
  };
}
```
改成：
```js
    window: { from: scan.minTs, to: scan.maxTs, files: scan.fileCount, messages: scan.msgCount },
  };
  const quests = questsByTaskInterval(scan, log);
  const tierCount = { D: 0, C: 0, B: 0, A: 0, S: 0 };
  let elitePoints = 0;
  for (const q of quests) { tierCount[q.tier]++; elitePoints += ELITE_WEIGHT[q.tier] || 0; }
  base.quests = quests;
  base.tierCount = tierCount;
  base.elitePoints = elitePoints;
  return base;
```
（同時把函式開頭 `return { ... }` 改為 `const base = { ... };`，最後 `return base;`。）

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/derive.test.cjs`
Expected: PASS（7 tests）。

- [ ] **Step 5: 加 exports + commit**

exports 追加 `classifyTier, questsByTaskInterval`。
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan1 委託區間歸屬+tier分級+精英分"
```

---

## Task 5: ③指揮 / ④殺敵 等級公式 ＋ streak（design §2.1 / §3.3）

**Files:**
- Modify: `exp.cjs`（新增 `commandLevel`、`slayLevel`、`computeStreak`；擴充 `deriveMetrics`）
- Test: `test/derive.test.cjs`

公式（design §2.1 定案）：指揮 Lv＝⌊√(對話次數/4)⌋；殺敵 Lv＝⌊√(打字字元/600)⌋。streak（design §3.3）：當日 ≥1 task event＝活躍；每週自動發 1 護符抵 1 天斷檔；當前 streak＋最長 PR。

- [ ] **Step 1: 寫失敗測試**

```js
test('commandLevel / slayLevel 平方根公式', () => {
  assert.strictEqual(exp.commandLevel(2129), 23); // ⌊√(2129/4)⌋ = ⌊23.07⌋
  assert.strictEqual(exp.slayLevel(1502000), 50); // ⌊√(1502000/600)⌋ = ⌊50.03⌋
  assert.strictEqual(exp.commandLevel(0), 0);
});

test('computeStreak 連續活躍日 + 護符抵斷', () => {
  // 活躍日：06-01,06-02,06-03 連 3；隔一天 06-05（斷1天，護符抵）；06-07 斷2天（護符已用→真斷）
  const mk = (d) => ({ ts: `2026-06-0${d} 10:00:00`, kind: 'task', reason: 'x' });
  const log = [1,2,3,5].map(mk); // 截到 06-05
  const r = exp.computeStreak(log, '2026-06-05');
  assert.strictEqual(r.current, 4);   // 06-01~03 + 護符補 06-04 + 06-05
  assert.strictEqual(r.longest >= 4, true);
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/derive.test.cjs`
Expected: FAIL — `exp.commandLevel is not a function`。

- [ ] **Step 3: 實作**

```js
function commandLevel(conversations) { return Math.floor(Math.sqrt(Math.max(0, conversations) / 4)); }
function slayLevel(typedChars) { return Math.floor(Math.sqrt(Math.max(0, typedChars) / 600)); }

// streak：當日 ≥1 task event = 活躍日。每「一週」期間自動發 1 枚護符，可抵 1 天斷檔。
// 從最後活躍日往回數連續鏈；單天空檔由當週護符抵掉不算斷，連空 ≥2 天（超出護符）即斷。
function computeStreak(log, todayStr) {
  const days = new Set(log.filter((e) => e.kind === 'task').map((e) => e.ts.slice(0, 10)));
  if (!days.size) return { current: 0, longest: 0 };
  const sorted = [...days].sort();
  const dayMs = 86400000;
  const toMs = (s) => Date.parse(s + ' 00:00:00');
  // longest：掃排序後相鄰差，≤1 天連、=2 天護符可補（每 7 天容 1 次）
  const runFrom = (startIdx, dir) => { /* helper not needed; compute below */ };
  // current：從 today 往回
  let current = 0; let tokens = 0; let weekAnchor = null;
  let cursor = toMs(todayStr);
  const activeSet = new Set(sorted.map(toMs));
  // 先確認 today 或昨天有活躍才起算
  while (true) {
    if (activeSet.has(cursor)) {
      current++;
      // 每跨 7 天活躍累積發 1 護符（簡化：每 7 個 current 給 1）
      cursor -= dayMs;
    } else {
      // 空檔：若還有護符可補（每週 1 枚，按已累積天數估），補 1 天繼續
      const earned = Math.floor(current / 7) + 1; // 含本週基礎 1 枚
      if (tokens < earned) { tokens++; cursor -= dayMs; }
      else break;
    }
    if (current > 100000) break; // 防爆
  }
  // longest：對排序活躍日做同規則連鏈
  let longest = 0, run = 0, tok2 = 0, prev = null;
  for (const ms of sorted.map(toMs)) {
    if (prev == null) { run = 1; tok2 = 0; }
    else {
      const gapDays = Math.round((ms - prev) / dayMs);
      if (gapDays === 1) run++;
      else if (gapDays === 2 && tok2 < Math.floor(run / 7) + 1) { tok2++; run++; }
      else { run = 1; tok2 = 0; }
    }
    if (run > longest) longest = run;
    prev = ms;
  }
  return { current, longest };
}
```

> **實作備註：** streak 護符規則 design §3.3 寫「每週自動發 1 枚、斷 1 天自動消耗」。上述為**可運作的近似**（每累積 7 活躍日給 1 枚、單天空檔用 1 枚補）。Plan 2 接面板前若需更貼近「日曆週」語意，於該 Plan 校準；本 Plan 先讓 `current/longest` 可算可測。

擴充 `deriveMetrics`（在 `return base` 前）：
```js
  base.commandLevel = commandLevel(base.conversations);
  base.slayLevel = slayLevel(base.typedChars);
  base.streak = computeStreak(log, _todayStr());
```
新增小工具（放 `now()` 附近）：
```js
function _todayStr() { return now().slice(0, 10); }
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/derive.test.cjs`
Expected: PASS（9 tests）。若 streak 近似邏輯與斷言不符，依「最後活躍日往回 + 護符抵單天」語意微調 `computeStreak`，使測試案例（current=4）通過。

- [ ] **Step 5: 加 exports + commit**

exports 追加 `commandLevel, slayLevel, computeStreak`。
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan1 ③指揮④殺敵公式 + streak"
```

---

## Task 6: `deriveAchievements` — 落地 achievements.json ＋ last_scanned_ts

**Files:**
- Modify: `exp.cjs`（新增 `paths` 加 `achievementsFile`；新增 `deriveAchievements`）
- Test: `test/derive.test.cjs`

- [ ] **Step 1: 寫失敗測試**

```js
test('deriveAchievements 寫 achievements.json + last_scanned_ts', () => {
  const home = fx.tmpHome();
  const root = fx.tmpProjects([{ proj: 'p', file: 's.jsonl', lines: [
    fx.asstMsg('2026-06-01T10:00:00.000Z', 'claude-opus-4-8', { in: 1000, out: 2000, cc: 0, cr: 0 }),
  ] }]);
  try {
    const p = exp.paths(home);
    const res = exp.deriveAchievements(p, { projectsRoot: root, log: [] });
    const fs = require('fs');
    const json = JSON.parse(fs.readFileSync(p.achievementsFile, 'utf8'));
    assert.strictEqual(json.derived.billable, 3000);
    assert.strictEqual(typeof json.last_scanned_ts, 'string');
    assert.strictEqual(json.derived.costUSD > 0, true);
  } finally { fx.rm(home); fx.rm(root); }
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test test/derive.test.cjs`
Expected: FAIL — `exp.deriveAchievements is not a function`（或 `p.achievementsFile` undefined）。

- [ ] **Step 3: 實作**

在 `paths()` 回傳物件加：
```js
    achievementsFile: path.join(base, 'achievements.json'),
```
新增函式：
```js
// 衍生引擎入口：掃 transcript + log → 算指標 → 寫 achievements.json（衍生快取，可 rebuild 重算）
// opts.projectsRoot 可注入（測試用）；opts.log 可注入，否則讀 p 的 log.jsonl
function deriveAchievements(p = paths(), opts = {}) {
  ensureBase(p);
  const root = opts.projectsRoot || projectsRoot();
  const log = opts.log || readLog(p);
  const scan = scanTranscripts(root);
  const derived = deriveMetrics(scan, log);
  const payload = {
    version: 'v2.5',
    scanned_at: now(),
    last_scanned_ts: scan.maxTs != null ? new Date(scan.maxTs).toISOString() : null,
    derived,
  };
  fs.writeFileSync(p.achievementsFile, JSON.stringify(payload, null, 2));
  return payload;
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test test/derive.test.cjs`
Expected: PASS（10 tests）。

- [ ] **Step 5: 加 exports + commit**

exports 追加 `deriveAchievements`。
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan1 deriveAchievements 落地 achievements.json"
```

---

## Task 7: CLI `derive` 指令 ＋ help

**Files:**
- Modify: `exp.cjs`（`main` switch 加 `derive` case；`HELP` 補一行）
- Test: 手動 CLI 驗證（無單元測試；屬整合）

- [ ] **Step 1: 加 CLI case**

在 `main` 的 switch（`rebuild` case 附近）加：
```js
    case 'derive': {
      const p = paths();
      const r = deriveAchievements(p);
      const d = r.derived;
      const 億 = (n) => (n / 1e8).toFixed(2) + '億';
      console.log(`✓ derive 完成 → ${p.achievementsFile}`);
      console.log(`計費等效 ${億(d.billable)}｜總處理量 ${億(d.totalProcessed)}｜成本 $${d.costUSD.toFixed(0)}`);
      console.log(`對話 ${d.conversations} → 指揮Lv${d.commandLevel}｜打字 ${(d.typedChars/10000).toFixed(1)}萬字(code ${(d.codePct*100).toFixed(0)}%) → 殺敵Lv${d.slayLevel}`);
      console.log(`委託 D${d.tierCount.D}/C${d.tierCount.C}/B${d.tierCount.B}/A${d.tierCount.A}/S${d.tierCount.S}｜精英分 ${d.elitePoints}｜streak ${d.streak.current}(PR${d.streak.longest})`);
      console.log(`使用時間 ${d.activeHours.toFixed(1)}hr｜窗 ${d.window.files} 檔 ${d.window.messages} 訊息`);
      break;
    }
```

`HELP` 維運區加一行：
```js
  維運：rebuild ｜ derive ｜ init ｜ help
```
（並在說明區加：`    derive                     掃 transcript 重算衍生指標 → achievements.json`）

- [ ] **Step 2: 跑全測試確認沒打破既有**

Run: `node --test test/derive.test.cjs`
Expected: PASS（10 tests 全綠）。

- [ ] **Step 3: 真實資料 smoke（核對 design 基準）**

Run（cwd = `_internal/payload/skills/ExpBook/`）：
```bash
EXPBOOK_HOME="$(mktemp -d)" node scripts/exp.cjs derive
```
Expected：計費等效 ≈ **2.0 億**、總處理量 ≈ **50 億**、成本 ≈ **$3,990**、對話 ≈ **2,129**、指揮 ≈ **Lv23**、打字 ≈ **208 萬字（code ~28%）**、殺敵 ≈ **Lv50**（對照 design 附錄 A）。委託數會與 design「session 級暫算」不同（本 Plan 是 task 級區間），屬預期差異。

> **驗收**：上述數量級吻合 design 附錄 A 即通過；若計費等效偏離 >10%，回查 `BILLABLE`／model 匹配／是否誤含 cache_read。

- [ ] **Step 4: Commit**

```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan1 CLI derive 指令 + help"
```

---

## Task 8: 效能實測 ＋ 部署驗證（design §5 效能列 plan 實測）

**Files:** 無程式碼變更；量測 + 部署。

- [ ] **Step 1: 量測全掃耗時**

Run：
```bash
EXPBOOK_HOME="$(mktemp -d)" bash -c 'time node scripts/exp.cjs derive'
```
記錄 real time。基準資料 238 sessions / 26,507 訊息。
- **判準**：< 3 秒 → 全掃可接受，增量留待後續優化（achievements.json 已存 `last_scanned_ts` 供日後增量用）。
- ≥ 3 秒 → 在 Plan 1 收尾記一筆 follow-up（增量掃描：只讀 mtime > last_scanned 的檔），但**不在本 Plan 實作**（YAGNI，先量了再說）。

- [ ] **Step 2: 部署到 live 並驗證**

Run：
```bash
cmd.exe /c _1.install.bat
```
然後比對 live 與 payload 一致：
```bash
diff ~/.claude/skills/ExpBook/scripts/exp.cjs _internal/payload/skills/ExpBook/scripts/exp.cjs && echo SYNCED
node ~/.claude/skills/ExpBook/scripts/exp.cjs derive
```
Expected：`SYNCED`；live 端 derive 輸出與 Task 7 Step 3 一致。

- [ ] **Step 3: 確認既有功能未壞（回歸）**

Run：
```bash
EXPBOOK_HOME="$(mktemp -d)" node scripts/exp.cjs task "回歸測試" --dungeon 測試 && node scripts/exp.cjs status
```
Expected：task 正常入帳（含新 `cwd` 欄）、status 正常輸出，三層面板不受影響。

- [ ] **Step 4: 收尾 commit（含 Repo A go/no-go）**

> Repo A（github PikaTool）：commit 前**單獨問使用者 go/no-go**（feedback-commit 鐵則）。
```bash
git add _internal/payload/skills/ExpBook/
git commit -m "feat(ExpBook): v2.5 Plan1 衍生引擎完成（效能實測+部署驗證）"
```

---

## Self-Review（對照 design spec）

**1. Spec 覆蓋：**
- §1 度量基準（計費等效排除 cr）→ `BILLABLE`/Task 2-3 ✓
- §2.1 ③④公式 → Task 5 ✓；②精英分 → Task 4 ✓
- §4.1 委託區間歸屬＋跨 session 合併＋界線 → Task 4 ✓；§4.4 edge（中間無 task 不形成委託）→ Task 4 邏輯 ✓
- §4.2 四分項 → Task 3 `flows` ✓（人話文案屬顯示層，Plan 2）
- §4.3 使用時間 gap<15／書本換算 → Task 3 `activeHours` ✓（書本換算純顯示，0.7 係數 Plan 2 套用）
- §4.4 計數限制（startsWith('<')／code%）→ Task 2 ✓
- §5 deriveAchievements/last_scanned_ts/效能 → Task 6/8 ✓
- §6 cwd → Task 1 ✓
- **本 Plan 不含**（屬 Plan 2/3）：①保持不動、⑤⑥儀表板顯示、一行面板、書本換算顯示、徽章/稱號/彩蛋/里程碑。✓ 邊界清楚。

**2. Placeholder 掃描：** 各 step 均有完整程式碼/指令/預期；streak 近似已明標備註非 placeholder。✓

**3. 型別一致：** `scanTranscripts` 回傳欄位（tok/byModel/billable/messages/tsList/userTurns/userChars/codeChars）在 Task 3-6 引用一致；`deriveMetrics` 回傳 `base.*` 欄位（billable/flows/conversations/typedChars/quests/tierCount/elitePoints/commandLevel/slayLevel/streak）在 Task 7 CLI 引用一致。✓

---

## 下一步

Plan 1 實作完 → 產 Plan 2（等級系統＋一行面板）續接 prompt。Plan 2 依賴本 Plan 的 `deriveMetrics` 輸出。
