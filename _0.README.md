# ExpBook — Agent 成長歷程系統

> 把每回合工作記成 EXP，產出玩家面板／週報／副本報告。看見成長歷程，不打考績。
> 本 repo 為 **ExpBook 獨立安裝包**（自 PikaTool 工具包抽出，保留完整 commit 歷史）。

## 工具一覽（共 1 個）

| # | 工具 | 版本 | 最後更新 | 用途 | 觸發方式 |
|---|------|------|---------|------|---------|
| 1 | **ExpBook** | v2.8.2 | 2026-09-23 09:51:28 | Agent 成長歷程系統：把工作記成 EXP，產出玩家面板／週報／副本報告 | hook（自動）+ skill |

## 安裝

需要 `python3`（安裝器）與 `node`（ExpBook hook / exp.cjs 執行）。

```sh
# Windows
_1.install.bat
# mac / Linux
sh _1.install.sh
```

安裝器（`_internal/scripts/install.py`，manifest 驅動、idempotent、跨平台）會：
- 複製 `skills/ExpBook/`（SKILL.md / GUIDE.md / scripts/exp.cjs）到 `~/.claude/`
- 複製 hook `expbook-prompt.cjs`（UserPromptSubmit）、`expbook-stop.cjs`（Stop）到 `~/.claude/hooks/`
- merge 進 `~/.claude/settings.json` 對應 hook 區段
- 建立 `expbook` 資料夾

## 核心哲學（不可違反）

1. 看見成長歷程，不打考績。
2. 正分階梯、只進不退（所有 EXP ≥ 0，衍生層等級只進不退）。
3. 厚程式、薄 AI（計算／渲染全在 `exp.cjs`；AI 每輪只辨識意圖→發一條短指令→轉述一行指標）。
4. 獎勵成果與學習、不獎勵掙扎、不刷分（衍生層純衍生·零 EXP 入口）。
5. 主角＝使用者本人；AI＝記錄引擎＋協作夥伴。

## 目錄

```
_0.README.md                          ← 本檔
CLAUDE.md                             ← 開發守門指引（改碼前必讀權威文件鏈）
_1.install.{bat,sh}                   ← 一鍵安裝
_internal/
  manifest.json                       ← 安裝清單（單一工具 expbook）
  scripts/install.py                  ← 安裝器（manifest 驅動）
  scripts/version-check.py            ← 版號一致性稽核（README ↔ SPEC）
  payload/skills/ExpBook/             ← skill 本體
    SKILL.md / GUIDE.md
    doc/SPEC.md                       ← 執行期權威規格
    scripts/exp.cjs                   ← 純 Node 零依賴實作
    test/*.test.cjs                   ← 測試
  payload/hooks/expbook-{prompt,stop}.cjs
docs/                                 ← 設計定稿 / 計畫 / 冒險者手冊
```

## 權威文件鏈（改任何數值/機制前先讀）

| 要改什麼 | 先讀 |
|---------|------|
| 任何修改第一站 | `_internal/payload/skills/ExpBook/doc/SPEC.md`（執行期權威規格） |
| 設計 rationale | `docs/superpowers/specs/2026-06-11-expbook-v2.5-design.md` |
| 實作 | `_internal/payload/skills/ExpBook/scripts/exp.cjs` |
| 人讀上手手冊 | `_internal/payload/skills/ExpBook/GUIDE.md` |

改完務必跑 `python _internal/scripts/version-check.py`（須 DRIFT 0）。
